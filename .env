# Ambiente al que apunta el app (QA por ahora).
# El portal QA/desplegado sirve el API en /api/v1/admin del mismo dominio.
# No contiene secretos: se versiona para que QA funcione con solo clonar.
EXPO_PUBLIC_API_URL=https://winspec.barecaonline.com/api/v1/admin
